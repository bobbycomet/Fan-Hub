# Fan Hub - Python Requirements
# Core UI
PyQt6>=6.4.0
PyQt6-Charts>=6.4.0

# Hardware monitoring
psutil>=5.9.0

# Liquid cooling (AIOs, USB controllers)
liquidctl>=1.13.0

# OpenRGB Python SDK (optional, falls back to CLI)
openrgb-python>=0.2.15

# Utilities
pyserial>=3.5        # Serial communication for some fan controllers
hidapi>=0.14.0       # HID device access (some RGB/fan controllers)
